# Assets

Place these two files here:

- `breakbeat-builder-wordmark.png` — displayed in the app header (max height: 32px)
- `breakbeat-builder-favicon.png` — used as the browser favicon

Until the wordmark image is added, the header shows a plain text fallback.
